from django.urls import path, include

app_name = "setup_base__OperationLevel"
base_path = "setup_base/OperationLevel/"


urlpatterns = []
#please add only path locations as they are only supported by package
       